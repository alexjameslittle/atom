# Android Templates

The initial bootstrap keeps the generated host files renderer-owned inside `atom-cng`.
This directory exists so future slices can move the Kotlin stubs into explicit templates without changing the repository contract from the spec.

