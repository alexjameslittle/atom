# References

Files this skill needs to inspect or execute:

- `scripts/verify.sh` — main verification harness
- `scripts/bootstrap.sh` — environment setup
- `.github/workflows/ci.yml` — CI pipeline (must stay aligned with local verification)
- `.bazelversion` — pinned Bazel version
- `MODULE.bazel` — dependency declarations
- `mise.toml` — developer toolchain versions
