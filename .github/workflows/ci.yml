name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  BUILDBUDDY_API_KEY: ${{ secrets.BUILDBUDDY_API_KEY }}
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  lint:
    name: lint
    runs-on: namespace-profile-linux
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Cache mise installs
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/mise
            ~/.local/share/mise
          key: ${{ runner.os }}-mise-${{ hashFiles('mise.toml', '.bazelversion') }}

      - name: Install mise
        run: |
          curl https://mise.run | sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Bootstrap toolchain
        run: |
          mise trust -y
          mise install

      - name: Setup BuildBuddy
        run: scripts/setup-buildbuddy.sh "$BUILDBUDDY_API_KEY"

      - name: Lint
        run: mise exec -- ./scripts/verify.sh lint

  test:
    name: test (${{ matrix.os }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: linux
            runner: namespace-profile-linux
          - os: macos
            runner: namespace-profile-macos
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Cache mise installs
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/mise
            ~/.local/share/mise
          key: ${{ runner.os }}-mise-${{ hashFiles('mise.toml', '.bazelversion') }}

      - name: Install mise
        run: |
          curl https://mise.run | sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Bootstrap toolchain
        run: |
          mise trust -y
          mise install

      - name: Setup BuildBuddy
        run: scripts/setup-buildbuddy.sh "$BUILDBUDDY_API_KEY"

      - name: Test
        run: mise exec -- ./scripts/verify.sh test

  build:
    name: build
    runs-on: namespace-profile-macos
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Cache mise installs
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/mise
            ~/.local/share/mise
          key: ${{ runner.os }}-mise-build-${{ hashFiles('mise.toml', '.bazelversion') }}

      - name: Install mise
        run: |
          curl https://mise.run | sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Bootstrap toolchain
        run: |
          mise trust -y
          mise install

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup BuildBuddy
        run: scripts/setup-buildbuddy.sh "$BUILDBUDDY_API_KEY"

      - name: Build apps
        run: mise exec -- ./scripts/verify.sh build
